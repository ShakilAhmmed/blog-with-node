
const config = {
    database: {
        DB_CONNECTION: "mysql",
        DB_HOST: "localhost",
        DB_PORT: "3306",
        DB_DATABASE: "blog_with_node",
        DB_USERNAME: "root",
        DB_PASSWORD: "",
    }
}

module.exports.config = config;